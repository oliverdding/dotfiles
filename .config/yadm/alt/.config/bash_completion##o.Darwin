eval "$(kaf completion bash)"
eval "$(helm completion bash)"
eval "$(limactl completion bash)"
eval "$(colima completion bash)"
eval "$(starship completion bash)"
# compat_dir=/opt/local/share/bash-completion/completions/
# if [[ -d $compat_dir && -r $compat_dir && -x $compat_dir ]]; then
#     for i in "$compat_dir"/*; do
#         [[ ${i##*/} != @($_backup_glob|Makefile*|$_blacklist_glob) && -f \
#         $i && -r $i ]] && . "$i"
#     done
# fi
# unset compat_dir i _blacklist_glob